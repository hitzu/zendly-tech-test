meta {
  name: get sorted
  type: http
  seq: 9
}

get {
  url: {{base_url}}/conversations?sort=newest
  body: json
  auth: inherit
}

params:query {
  sort: newest
}

headers {
  authorization: Bearer {{tokenAdminInbox1}}
}

settings {
  encodeUrl: true
  timeout: 0
}
