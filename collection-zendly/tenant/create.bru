meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{base_url}}/tenants
  body: json
  auth: inherit
}

body:json {
  {
    "name":"Hitzu Labs"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
