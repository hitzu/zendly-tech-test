meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "operatorId": 8
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
