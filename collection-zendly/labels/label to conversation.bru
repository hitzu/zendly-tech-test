meta {
  name: label to conversation
  type: http
  seq: 4
}

post {
  url: {{base_url}}/conversations/:conversationId/labels/:labelId
  body: json
  auth: inherit
}

params:path {
  conversationId: 5
  labelId: 1
}

headers {
  authorization: Bearer {{tokenOperatorInbox1}}
}

settings {
  encodeUrl: true
  timeout: 0
}
