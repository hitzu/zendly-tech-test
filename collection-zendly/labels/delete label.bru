meta {
  name: delete label
  type: http
  seq: 8
}

delete {
  url: {{base_url}}/labels/2
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{tokenOperatorInbox1}}
}

settings {
  encodeUrl: true
  timeout: 0
}
