meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{base_url}}/operator-inbox-subscriptions
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{tokenAdmin}}
}

body:json {
  {
    "operatorId":6,
    "inboxId":2
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
