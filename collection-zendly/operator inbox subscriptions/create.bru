meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{base_url}}/operator-inbox-subscriptions
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "operatorId":2,
    "inboxId":1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
